This directory will serve as a placeholder for database information until I (Robert) can figure out how to properly store MySQL server files on github (for the sake of centralization).
I might also keep it for ease of use compared to setting up and running a proper server to design database elements (before I eventually move over to MySQL anyway).

As a result of the aforementioned, this directory will be deleted eventually, though it will not be removed without warning.
